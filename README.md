# Image AI Nest React Docker

## Using Docker compose

### Set env variables

```bash
cp .env.example .env
```

Set the environment variables in the `.env` file.

### Build the image

```bash
docker-compose build
```

### Run the container

```bash
docker-compose up -d
```
